#include<iostream>
using namespace std;
int main()
{
	int a;
	cout<<"enter the first number:";
	cin>>a;
	if(a<0)
	cout<<a<<"is negative";
	else if(a<0)
	cout<<a<<"is a positive";
	else
	cout<<a<<"is a zero";
	return 0;
}
OUTPUT:
enter the first number:-4
-4is negative
